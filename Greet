<!DOCTYPE html>
<html lang="ur">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Aap Ke Liye Paigham!</title>
    <link rel="stylesheet" href="style.css">
</head>
<body>
    <div class="card-container">
        <div class="card-front" id="front-card">
            <h1>👋 Aik Paigham!</h1>
            <p>Khulne ke liye **Click** karein!</p>
        </div>

        <div class="card-inside" id="inside-card">
            <h2 id="greeting-text">Aapko Salam!</h2>
            <p>*** (Wishes for Recipient's Name) ***</p>
            <p class="wishes-message">
                Aapki zindagi khushiyon aur kamyabi se bharpur ho. <br>
                Hamesha muskurate rahen!
            </p>
            <p class="sender">Aapka dost/khairkhwah, [Aapka Naam]</p>
        </div>
    </div>

    <script src="script.js"></script>
</body>
</html>
body {
    display: flex;
    justify-content: center;
    align-items: center;
    min-height: 100vh;
    margin: 0;
    background-color: #f0f0f0; /* Halka background */
    font-family: Arial, sans-serif;
    perspective: 1000px; /* 3D effect ke liye */
}

.card-container {
    width: 300px;
    height: 400px;
    position: relative;
    transform-style: preserve-3d;
    transition: transform 1s; /* Card khulne ki speed */
    cursor: pointer;
}

/* Jab card khulega (JavaScript se yeh class add hogi) */
.card-container.open {
    transform: rotateY(180deg);
}

.card-front, .card-inside {
    width: 100%;
    height: 100%;
    position: absolute;
    backface-visibility: hidden; /* Peeche ka hissa chhupane ke liye */
    border-radius: 15px;
    box-shadow: 0 10px 20px rgba(0, 0, 0, 0.3);
    display: flex;
    flex-direction: column;
    justify-content: center;
    align-items: center;
    text-align: center;
    padding: 20px;
}

.card-front {
    background-color: #ff99aa; /* Pink color */
    color: white;
    z-index: 2;
}

.card-inside {
    background-color: white;
    color: #333;
    transform: rotateY(180deg); /* Shuru mein ulta (reverse) rakhenge */
}

.card-inside h2 {
    color: #d9534f;
    font-size: 2em;
    animation: bounceIn 1s ease-out; /* Animation */
}

.wishes-message {
    margin: 20px 0;
    line-height: 1.6;
}

.sender {
    margin-top: 30px;
    font-style: italic;
    color: #777;
}

/* Choti si animation CSS mein */
@keyframes bounceIn {
    0% { transform: scale(0.1); opacity: 0; }
    60% { transform: scale(1.2); opacity: 1; }
    100% { transform: scale(1); }
}
document.addEventListener('DOMContentLoaded', () => {
    const cardContainer = document.querySelector('.card-container');
    const insideCard = document.getElementById('inside-card');
    const greetingText = document.getElementById('greeting-text');

    // 1. URL se naam nikalna (For personalization)
    // Agar link aisa ho: yourlink.com?name=Ali
    const urlParams = new URLSearchParams(window.location.search);
    const recipientName = urlParams.get('name');

    // 2. Greeting/Wishes ko customize karna
    let nameForMessage = "Mehboob"; // Default name
    
    if (recipientName) {
        // Agar URL mein naam diya gaya hai
        nameForMessage = recipientName.replace(/\+/g, ' '); // Plus signs ko space se replace karein
        greetingText.textContent = `Aapko Bohat Mubarak, ${nameForMessage}!`; 
        
        // Best Wishes message mein bhi naam dal sakte hain
        const wishesMessage = document.querySelector('.wishes-message');
        wishesMessage.innerHTML = `**${nameForMessage}**, Aapki zindagi khushiyon aur kamyabi se bharpur ho. <br> Hamesha muskurate rahen!`;
    } else {
        // Agar URL mein koi naam nahi hai
        greetingText.textContent = `Aapke Liye Salam!`;
    }


    // 3. Card ko click par kholna
    cardContainer.addEventListener('click', () => {
        cardContainer.classList.toggle('open');
    });
});
